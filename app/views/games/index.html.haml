%h1
  Games
%table
  %thead
    %tr
      %th ID
      %th Status
      %th Typ
    - for game in @games
      %tr
        %td
          = link_to game.id, resource(game)
        %td
          = game.state
        %td
          = game.type
